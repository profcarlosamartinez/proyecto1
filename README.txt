## Requisitos previos
Instalar MariaDb con docker-compose
Usar docker-compose.yml 
Para crear contenedor: docker-compose up -d

## Instalación
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python __main__.py